&НаСервере
Процедура ЧтениеНаСервере(Ном)
	 Отбор = Новый Структура ("Номенклатура",Ном);
	Выборка = РегистрыСведений.ЦенаНоменклатуры.ПолучитьПоследнее(ТекущаяДата(),Отбор);
	Сообщить(Выборка.Цена);
	//Пока Выборка.Следующий()Цикл 
	//	Сообщить(Строка(Выборка.Номенклатура) + " = " + Строка(Выборка.Цена));
	//КонецЦикла;

КонецПроцедуры

&НаКлиенте
Процедура Чтение(Команда) 
	ТекСтрока = Элементы.Список.ТекущиеДанные;
	ЧтениеНаСервере(ТекСтрока.Номенклатура);
КонецПроцедуры

