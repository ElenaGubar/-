&НаСервере
Процедура СформироватьНаСервере()
	ОтчетОбъект = РеквизитФормыВЗначение("Отчет");  // Получим заготовку отчета 
	СтруктураПараметров = Новый Структура("ЧастьНаименования",ЧастьНаименования); 
	ТабДокумент = ОтчетОбъект.Сформировать(СтруктураПараметров);  //ЗначениеВРеквизитеФормы(ОтчетОбъект, "Отчет");
КонецПроцедуры

&НаКлиенте
Процедура Сформировать(Команда)
	СформироватьНаСервере();
КонецПроцедуры

